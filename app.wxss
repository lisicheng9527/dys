@import "./iconfont/iconfont.wxss";

page {
  --themeColor:#643C2C;
}

view {
  font-family: Arial, sans-serif;
}
.header {
  height: 40px;
  text-align: center;
  line-height: 40px;
  font-size: 20px;
  color: var(--themeColor);
  position: relative;
}
